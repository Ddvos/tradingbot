# Strategy configs

YAML configuration per strategy — **one file per strategy/hypothesis**.
Loaded + validated via Pydantic at the system boundary (no raw dicts).

The first example config follows in Slice 1 (see `ROADMAP.md`).
