"""Risk — position sizing (pure functions) + RiskManager (class).

sizing.py: position_size(balance, atr, risk_pct) -> Decimal (Slice 1).
manager.py: limits, consecutive losses, daily P&L, circuit breakers (Slice 6).
"""
